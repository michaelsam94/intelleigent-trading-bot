# Minimal deps for scripts/eth_ta_telegram.py on Railway (Dockerfile installs TA-Lib C library).
# Do not use full requirements.txt here — it pulls TensorFlow/LightGBM etc. and breaks slim deploys.

numpy>=2.0,<3
pandas>=2.0,<3
python-binance>=1.0.0,<2
requests>=2.28
TA-Lib
google-genai>=0.7.0
google-generativeai>=0.8.0
PySocks>=1.7.1
